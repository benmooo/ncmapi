package necmapi_test

import "testing"

func TestRelatedPlaylist(t *testing.T) {
	// res, err := api.RelatedPlaylist(collectionID)
	// if err != nil {
	// 	t.Error(err)
	// }

	// if res.Code != 200 {
	// 	t.Errorf("code: %d, msg: %s, message: %s, time: %d", res.Code, res.Msg, res.Message, res.Time)
	// }
	t.Log("TestRelatedPlaylist: TODO")
}
